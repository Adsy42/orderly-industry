# Supabase Configuration
# Get these from your Supabase project settings → API
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# LangGraph Agent Configuration
# For local development, point to the local agent server
NEXT_PUBLIC_API_URL=http://localhost:2024
# For production with API passthrough, use your domain + /api
# NEXT_PUBLIC_API_URL=https://your-domain.com/api

# The graph ID from your agent's langgraph.json
NEXT_PUBLIC_ASSISTANT_ID=deep_research

# LangSmith Configuration (for API passthrough in production)
# These are server-side only - do NOT prefix with NEXT_PUBLIC_
LANGGRAPH_API_URL=https://your-agent.default.us.langgraph.app
LANGSMITH_API_KEY=lsv2_pt_your-key
